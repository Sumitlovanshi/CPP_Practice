#include<stdio.h>
void main()
{
    int i,ft,cd,n,N,diff,term;
    printf("enter the term to be calculated");
    scanf("%d",&n);
    for(i=1;i<=n;i++)
        if(n%2==0)
          { ft=4;
            cd=((n-1)/2);
            diff=cd*4;
            diff=ft+diff;
          }
        else
            {   ft=3;
                cd=(n/2);
                diff=cd*4;
                diff=ft+diff;
            }
printf("the nth term is %d",diff);
}
